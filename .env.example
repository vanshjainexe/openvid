# Optional - only needed for the in-editor photo picker (Unsplash/Pexels/Pixabay).
# All keys are read server-side, so they never reach the client bundle.
# Without these the app still runs; only the photo picker returns empty results.
UNSPLASH_ACCESS_KEY=
PEXELS_API_KEY=
PIXABAY_API_KEY=
